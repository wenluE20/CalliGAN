# -*- coding: utf-8 -*-

def get_cangjie(char):
	# 由于缺少cns_char.txt和CNS_cangjie.txt文件，返回默认值
	# 这样可以避免文件不存在的错误
	return None


def get_component(char):
	# 由于缺少cns_char.txt和CNS_component.txt文件，返回默认值
	# 这样可以避免文件不存在的错误
	return None


